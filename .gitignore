# Godot
.godot/
*.translation
build/
# export_presets.cfg è versionato di proposito: niente credenziali dentro,
# solo i preset desktop (le identità di firma vanno tenute fuori dal repo).

# .NET
bin/
obj/
*.user
.mono/
mono_crash.*.json

# macOS
.DS_Store
